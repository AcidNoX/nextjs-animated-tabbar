import { motion } from "framer-motion";
import styled from "styled-components";

export const Clip = styled(motion.circle)`
  transform-origin: 50% 50%; 
`;

export const Svg = styled.svg`
  width: 100%;
  height: 100%;
  overflow: visible;
`;